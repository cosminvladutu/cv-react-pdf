/**
 * RightSection/index.ts
 * 
 * This index file exports the RightSection component as the default export.
 * Using this barrel file pattern simplifies imports in other components,
 * allowing for cleaner code structure and better organization.
 * 
 * Usage example:
 * ```
 * import RightSection from '../RightSection';
 * ```
 */
import RightSection from "./RightSection";

export default RightSection;
